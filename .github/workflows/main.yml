name: Deploy to FTP
on: push

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Deploy to FTP
      uses: peaceiris/actions-ftp-client@v2
      with:
        host: win5206.site4now.net
        username: hoang3409-001
        password: ${{ secrets.FTP_PASSWORD }}
        remote-dir: /root/home/hoang3409-001/www/booklibrary
        clean: true
